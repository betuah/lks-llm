{
   "name": "english-apps",
   "version": "1.0.0",
   "type": "module",
   "engines": {
      "node": ">=20.0.0"
   },
   "scripts": {
      "deploy": "serverless deploy",
      "install:langchain": "cd src/layers/langchain/nodejs && npm install",
      "install:assessment": "cd src/layers/assessment/nodejs && npm install",
      "install:layers": "npm run install:langchain && npm run install:assessment",
      "postinstall": "npm run install:layers"
   },
   "devDependencies": {
      "serverless": "^3.38.0"
   }
}
